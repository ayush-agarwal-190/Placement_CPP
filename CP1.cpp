#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<int> vi;
typedef vector<vi> vvi;
typedef vector<ll> vll;
typedef pair<int,int> pii;
typedef pair<long,long> pll;
typedef vector<pii> vpii;

#define REP(i,x,y) for(int i=x,i<y;i++)
#define F first
#define S second
#define PB push_back
#define EB emplace_back // eplace back is faster than pushback
#define PRES(c,x)((c).find(x)!=(c).end())



int main()
{
// Turn of the synchronization of cpp standarsd
// stream with corresponding c streams

ios_base::sync_with_stdio(false);
//Unites input and output (No Flushing) happens
cin.tie(NULL);
//Endl causes flush operation
cout<<"GFG"<<"\n";
}